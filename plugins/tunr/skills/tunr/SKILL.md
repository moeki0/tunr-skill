---
description: ユーザーの画面・音声コンテキストを検索・参照するスキル。ユーザーが「さっき見てたやつ」「画面に出てた」「ブラウザで開いてた」「さっき聞いてた」「動画で言ってた」など画面や音声の内容に言及したとき、またはユーザーの作業文脈を理解するのに画面・音声情報が役立ちそうなときに自動発動する。
---

# tunr — 画面・音声コンテキスト参照

ユーザーが画面で見ている情報や聞いている音声を検索・参照する。tunr watch デーモンが記録した画面履歴・音声文字起こしをMCPツールで検索する。

## いつ使うか

- ユーザーが「さっき見てた」「画面に出てた」「ブラウザで開いてた」「あのページ」など画面コンテキストに言及したとき
- ユーザーが「さっき聞いてた」「動画で言ってた」「ミーティングで話してた」など音声コンテキストに言及したとき
- ユーザーの発言の背景を理解するために画面・音声情報が役立ちそうなとき
- ユーザーが何かを参照しているが、具体的なURLやファイルパスを示していないとき

## 手順

1. まず `recent_screens` ツールで直近の画面状態を確認する（デフォルト10分）
2. 特定のアプリやウィンドウに絞る場合は `app` パラメータを使う（例: `app: "Chrome"`）
3. 特定のキーワードで探す場合は `search_screen_history` ツールを使う
4. 音声に関する質問の場合は `recent_audio` で直近の文字起こしを確認する
5. 特定のキーワードで音声を探す場合は `search_audio` ツールを使う
6. リアルタイムで画面を追跡したい場合は `toggle_tv(enabled: true)` でTV通知を有効化する
7. リアルタイムで音声を追跡したい場合は `toggle_radio(enabled: true)` でRADIO通知を有効化する
7. 取得したコンテキストを基に、ユーザーの意図を理解して回答する

## MCP ツール

- `search_screen_history(query, app?, minutes?, limit?)` — 画面テキストをキーワード検索。app名、ウィンドウタイトル、表示テキストが対象。`app` でアプリ/ウィンドウ名フィルタ
- `recent_screens(app?, minutes?, limit?)` — 直近N分の画面状態一覧。`app` でアプリ/ウィンドウ名フィルタ
- `recent_audio(minutes?, limit?)` — 直近N分の音声文字起こし一覧
- `search_audio(query, minutes?, limit?)` — 音声文字起こしをキーワード検索
- `toggle_tv(enabled)` — 画面変化のリアルタイムチャンネル通知（TV）をON/OFF
- `toggle_radio(enabled)` — 音声文字起こしのリアルタイムチャンネル通知（RADIO）をON/OFF

## チャンネルイベント

- `user_send` — ユーザーがショートカットで現在の画面を共有
- `tv` — リアルタイム画面変化通知（`toggle_tv` で有効化が必要）
- `radio` — リアルタイム音声文字起こし（`toggle_radio` で有効化が必要）

## 注意

- tunr watch デーモンが起動していない場合、ツールは「No screen/audio history available」を返す。その場合はユーザーに `tunr watch` の起動を提案する
- 画面データはユーザーが許可したウィンドウのみ記録されている
- 音声キャプチャにはBlackHoleとマルチ出力デバイスの設定が必要
