---
description: ユーザーの画面コンテキストを検索・参照するスキル。ユーザーが「さっき見てたやつ」「画面に出てた」「ブラウザで開いてた」など画面の内容に言及したとき、またはユーザーの作業文脈を理解するのに画面情報が役立ちそうなときに自動発動する。
---

# uitocc — 画面コンテキスト参照

ユーザーが画面で見ている情報を検索・参照する。uitocc watch デーモンが記録した画面履歴をMCPツールで検索する。

## いつ使うか

- ユーザーが「さっき見てた」「画面に出てた」「ブラウザで開いてた」「あのページ」など画面コンテキストに言及したとき
- ユーザーの発言の背景を理解するために画面情報が役立ちそうなとき
- ユーザーが何かを参照しているが、具体的なURLやファイルパスを示していないとき

## 手順

1. まず `recent_screens` ツールで直近の画面状態を確認する（デフォルト10分）
2. 特定のキーワードで探す場合は `search_screen_history` ツールを使う
3. 取得した画面コンテキストを基に、ユーザーの意図を理解して回答する

## MCP ツール

- `search_screen_history(query, minutes?, limit?)` — 画面テキストをキーワード検索。app名、ウィンドウタイトル、表示テキストが対象
- `recent_screens(minutes?, limit?)` — 直近N分の画面状態一覧

## 注意

- uitocc watch デーモンが起動していない場合、ツールは「No screen history available」を返す。その場合はユーザーに `uitocc watch` の起動を提案する
- 画面データはユーザーが許可したウィンドウのみ記録されている
